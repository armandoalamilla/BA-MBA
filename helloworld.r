#Create dataframe
libros=data.frame(
   "id"=c(1,2,3,4),
   "libros"=c("papalota", "dan brown", "la fortaleza", "libro playero")
  )

#Display the dataframe
print(libros)

trenmaya <- "se descarrillo"
print(trenmaya)